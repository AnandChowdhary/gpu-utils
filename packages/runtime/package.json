{
  "name": "@gpu-utils/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Shared WebGPU runtime primitives for gpu-utils packages (bundled into each package at build time)",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./wgsl/*": "./src/wgsl/*"
  },
  "scripts": {
    "test": "vitest run"
  }
}

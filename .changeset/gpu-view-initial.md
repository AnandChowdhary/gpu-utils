---
"gpu-view": minor
---

Initial release of gpu-view: natural language to table view specs (filter, sort, group, aggregate, limit, chart). A 33K-parameter schema-blind affine-scan tagger that runs on WebGPU or the CPU, with a deterministic TypeScript compiler that resolves fields, dates and numbers and reports diagnostics instead of guessing.
